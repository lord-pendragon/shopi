import { ShoppingCart, AccountCircle } from "@mui/icons-material";
import { Box, styled, Button, Grid, Typography, Menu, MenuItem } from "@mui/material";



export const Login = styled(Box)({
    
})

export const LoginItems = styled(Grid)({

})

export const LoginItem = styled(Grid)({
    
})

export const CartIcon = styled(ShoppingCart)({
    cursor: 'pointer'
})

export const UserIcon = styled(AccountCircle)({
    cursor: 'pointer'
})

export const SignInButton = styled(Button)({

})

export const SignInText = styled(Typography)({

})

export const UserMenu = styled(Menu)({
})

export const UserMenuOption = styled(MenuItem)({
})